# OZ Shop 0.7.1

- Condition-confirmation sales now remain bounded to the requested amount and are revalidated before settlement.
- Automatic drain uses the current scoped stock, with `drainMax` retained as the cap.
- `restockMax` is now the fixed amount added per economy tick. Old restock-rate JSON input remains readable and is ignored.
