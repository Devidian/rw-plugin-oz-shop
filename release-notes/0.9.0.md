# OZ Shop 0.9.0

- Adds a stable per-item base-price lookup from the complete system-offer catalog for compatible OZ plugins.
- The lookup is cached on Shop reload and returns zero for items without a catalog base price.
